# momentum-ml-ic-validated
Random Forest stock selection on top of a momentum pre-filter, backtested 2022-2025 with walk-forward retraining. Validated using Information Coefficient (not just returns) - the static model shows ~zero genuine skill while the retrained version shows a small but real edge.
